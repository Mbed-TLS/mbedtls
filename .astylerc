# Brace style:
# |int foo()
# |{
# |    if (condition) {
# |        stuff();
# |    }
# |}
--style=kr

# Mandate braces for one-line compound statements.
# |if (condition) {
# |    foo();
# |}
--add-braces

# Parenthesis whitespace style:
# |if (condition) function(arguments);
--unpad-paren
--pad-header

# Indentation inside parentheses:
# |if (one ||
# |    two);
--min-conditional-indent=0

# Pointer space style:
# |int *p;
--align-pointer=name
