/* Automatically generated by generate_verify_psa_macros.py. DO NOT EDIT. */

#include <stdio.h>

#include "psa/crypto.h"

#ifdef PSA_ALG_AEAD_WITH_DEFAULT_LENGTH_TAG
#define SPEC_PSA_ALG_AEAD_WITH_DEFAULT_LENGTH_TAG(aead_alg) \
        ((((aead_alg) & ~0x003f0000) == 0x05400100) ? PSA_ALG_CCM : \
         (((aead_alg) & ~0x003f0000) == 0x05400200) ? PSA_ALG_GCM : \
         (((aead_alg) & ~0x003f0000) == 0x05000500) ? PSA_ALG_CHACHA20_POLY1305 : \
         PSA_ALG_NONE)
#else
#define PSA_ALG_AEAD_WITH_DEFAULT_LENGTH_TAG(aead_alg) \
        ((((aead_alg) & ~0x003f0000) == 0x05400100) ? PSA_ALG_CCM : \
         (((aead_alg) & ~0x003f0000) == 0x05400200) ? PSA_ALG_GCM : \
         (((aead_alg) & ~0x003f0000) == 0x05000500) ? PSA_ALG_CHACHA20_POLY1305 : \
         PSA_ALG_NONE)
#endif

#ifdef PSA_ALG_AEAD_WITH_SHORTENED_TAG
#define SPEC_PSA_ALG_AEAD_WITH_SHORTENED_TAG(aead_alg, tag_length) \
        ((psa_algorithm_t) (((aead_alg) & ~0x003f0000) | (((tag_length) & 0x3f) << 16)))
#else
#define PSA_ALG_AEAD_WITH_SHORTENED_TAG(aead_alg, tag_length) \
        ((psa_algorithm_t) (((aead_alg) & ~0x003f0000) | (((tag_length) & 0x3f) << 16)))
#endif

#ifdef PSA_ALG_ANY_HASH
#define SPEC_PSA_ALG_ANY_HASH ((psa_algorithm_t)0x020000ff)
#else
#define PSA_ALG_ANY_HASH ((psa_algorithm_t)0x020000ff)
#endif

#ifdef PSA_ALG_CBC_MAC
#define SPEC_PSA_ALG_CBC_MAC ((psa_algorithm_t)0x03c00100)
#else
#define PSA_ALG_CBC_MAC ((psa_algorithm_t)0x03c00100)
#endif

#ifdef PSA_ALG_CBC_NO_PADDING
#define SPEC_PSA_ALG_CBC_NO_PADDING ((psa_algorithm_t)0x04404000)
#else
#define PSA_ALG_CBC_NO_PADDING ((psa_algorithm_t)0x04404000)
#endif

#ifdef PSA_ALG_CBC_PKCS7
#define SPEC_PSA_ALG_CBC_PKCS7 ((psa_algorithm_t)0x04404100)
#else
#define PSA_ALG_CBC_PKCS7 ((psa_algorithm_t)0x04404100)
#endif

#ifdef PSA_ALG_CCM
#define SPEC_PSA_ALG_CCM ((psa_algorithm_t)0x05500100)
#else
#define PSA_ALG_CCM ((psa_algorithm_t)0x05500100)
#endif

#ifdef PSA_ALG_CFB
#define SPEC_PSA_ALG_CFB ((psa_algorithm_t)0x04c01100)
#else
#define PSA_ALG_CFB ((psa_algorithm_t)0x04c01100)
#endif

#ifdef PSA_ALG_CHACHA20_POLY1305
#define SPEC_PSA_ALG_CHACHA20_POLY1305 ((psa_algorithm_t)0x05100500)
#else
#define PSA_ALG_CHACHA20_POLY1305 ((psa_algorithm_t)0x05100500)
#endif

#ifdef PSA_ALG_CMAC
#define SPEC_PSA_ALG_CMAC ((psa_algorithm_t)0x03c00200)
#else
#define PSA_ALG_CMAC ((psa_algorithm_t)0x03c00200)
#endif

#ifdef PSA_ALG_CTR
#define SPEC_PSA_ALG_CTR ((psa_algorithm_t)0x04c01000)
#else
#define PSA_ALG_CTR ((psa_algorithm_t)0x04c01000)
#endif

#ifdef PSA_ALG_DETERMINISTIC_ECDSA
#define SPEC_PSA_ALG_DETERMINISTIC_ECDSA(hash_alg) \
        ((psa_algorithm_t) (0x06000700 | ((hash_alg) & 0x000000ff)))
#else
#define PSA_ALG_DETERMINISTIC_ECDSA(hash_alg) \
        ((psa_algorithm_t) (0x06000700 | ((hash_alg) & 0x000000ff)))
#endif

#ifdef PSA_ALG_ECB_NO_PADDING
#define SPEC_PSA_ALG_ECB_NO_PADDING ((psa_algorithm_t)0x04404400)
#else
#define PSA_ALG_ECB_NO_PADDING ((psa_algorithm_t)0x04404400)
#endif

#ifdef PSA_ALG_ECDH
#define SPEC_PSA_ALG_ECDH ((psa_algorithm_t)0x09020000)
#else
#define PSA_ALG_ECDH ((psa_algorithm_t)0x09020000)
#endif

#ifdef PSA_ALG_ECDSA
#define SPEC_PSA_ALG_ECDSA(hash_alg) \
        ((psa_algorithm_t) (0x06000600 | ((hash_alg) & 0x000000ff)))
#else
#define PSA_ALG_ECDSA(hash_alg) \
        ((psa_algorithm_t) (0x06000600 | ((hash_alg) & 0x000000ff)))
#endif

#ifdef PSA_ALG_ECDSA_ANY
#define SPEC_PSA_ALG_ECDSA_ANY ((psa_algorithm_t) 0x06000600)
#else
#define PSA_ALG_ECDSA_ANY ((psa_algorithm_t) 0x06000600)
#endif

#ifdef PSA_ALG_FFDH
#define SPEC_PSA_ALG_FFDH ((psa_algorithm_t)0x09010000)
#else
#define PSA_ALG_FFDH ((psa_algorithm_t)0x09010000)
#endif

#ifdef PSA_ALG_FULL_LENGTH_MAC
#define SPEC_PSA_ALG_FULL_LENGTH_MAC(mac_alg) \
        ((psa_algorithm_t) ((mac_alg) & ~0x003f0000))
#else
#define PSA_ALG_FULL_LENGTH_MAC(mac_alg) \
        ((psa_algorithm_t) ((mac_alg) & ~0x003f0000))
#endif

#ifdef PSA_ALG_GCM
#define SPEC_PSA_ALG_GCM ((psa_algorithm_t)0x05500200)
#else
#define PSA_ALG_GCM ((psa_algorithm_t)0x05500200)
#endif

#ifdef PSA_ALG_HKDF
#define SPEC_PSA_ALG_HKDF(hash_alg) \
        ((psa_algorithm_t) (0x08000100 | ((hash_alg) & 0x000000ff)))
#else
#define PSA_ALG_HKDF(hash_alg) \
        ((psa_algorithm_t) (0x08000100 | ((hash_alg) & 0x000000ff)))
#endif

#ifdef PSA_ALG_HMAC
#define SPEC_PSA_ALG_HMAC(hash_alg) \
        ((psa_algorithm_t) (0x03800000 | ((hash_alg) & 0x000000ff)))
#else
#define PSA_ALG_HMAC(hash_alg) \
        ((psa_algorithm_t) (0x03800000 | ((hash_alg) & 0x000000ff)))
#endif

#ifdef PSA_ALG_KEY_AGREEMENT
#define SPEC_PSA_ALG_KEY_AGREEMENT(ka_alg, kdf_alg) \
        ((ka_alg) | (kdf_alg))
#else
#define PSA_ALG_KEY_AGREEMENT(ka_alg, kdf_alg) \
        ((ka_alg) | (kdf_alg))
#endif

#ifdef PSA_ALG_MD2
#define SPEC_PSA_ALG_MD2 ((psa_algorithm_t)0x02000001)
#else
#define PSA_ALG_MD2 ((psa_algorithm_t)0x02000001)
#endif

#ifdef PSA_ALG_MD4
#define SPEC_PSA_ALG_MD4 ((psa_algorithm_t)0x02000002)
#else
#define PSA_ALG_MD4 ((psa_algorithm_t)0x02000002)
#endif

#ifdef PSA_ALG_MD5
#define SPEC_PSA_ALG_MD5 ((psa_algorithm_t)0x02000003)
#else
#define PSA_ALG_MD5 ((psa_algorithm_t)0x02000003)
#endif

#ifdef PSA_ALG_NONE
#define SPEC_PSA_ALG_NONE ((psa_algorithm_t)0)
#else
#define PSA_ALG_NONE ((psa_algorithm_t)0)
#endif

#ifdef PSA_ALG_OFB
#define SPEC_PSA_ALG_OFB ((psa_algorithm_t)0x04c01200)
#else
#define PSA_ALG_OFB ((psa_algorithm_t)0x04c01200)
#endif

#ifdef PSA_ALG_RIPEMD160
#define SPEC_PSA_ALG_RIPEMD160 ((psa_algorithm_t)0x02000004)
#else
#define PSA_ALG_RIPEMD160 ((psa_algorithm_t)0x02000004)
#endif

#ifdef PSA_ALG_RSA_OAEP
#define SPEC_PSA_ALG_RSA_OAEP(hash_alg) \
        ((psa_algorithm_t)(0x07000300 | ((hash_alg) & 0x000000ff)))
#else
#define PSA_ALG_RSA_OAEP(hash_alg) \
        ((psa_algorithm_t)(0x07000300 | ((hash_alg) & 0x000000ff)))
#endif

#ifdef PSA_ALG_RSA_PKCS1V15_CRYPT
#define SPEC_PSA_ALG_RSA_PKCS1V15_CRYPT ((psa_algorithm_t)0x07000200)
#else
#define PSA_ALG_RSA_PKCS1V15_CRYPT ((psa_algorithm_t)0x07000200)
#endif

#ifdef PSA_ALG_RSA_PKCS1V15_SIGN
#define SPEC_PSA_ALG_RSA_PKCS1V15_SIGN(hash_alg) \
        ((psa_algorithm_t)(0x06000200 | ((hash_alg) & 0x000000ff)))
#else
#define PSA_ALG_RSA_PKCS1V15_SIGN(hash_alg) \
        ((psa_algorithm_t)(0x06000200 | ((hash_alg) & 0x000000ff)))
#endif

#ifdef PSA_ALG_RSA_PKCS1V15_SIGN_RAW
#define SPEC_PSA_ALG_RSA_PKCS1V15_SIGN_RAW ((psa_algorithm_t) 0x06000200)
#else
#define PSA_ALG_RSA_PKCS1V15_SIGN_RAW ((psa_algorithm_t) 0x06000200)
#endif

#ifdef PSA_ALG_RSA_PSS
#define SPEC_PSA_ALG_RSA_PSS(hash_alg) \
        ((psa_algorithm_t)(0x06000300 | ((hash_alg) & 0x000000ff)))
#else
#define PSA_ALG_RSA_PSS(hash_alg) \
        ((psa_algorithm_t)(0x06000300 | ((hash_alg) & 0x000000ff)))
#endif

#ifdef PSA_ALG_SHA3_224
#define SPEC_PSA_ALG_SHA3_224 ((psa_algorithm_t)0x02000010)
#else
#define PSA_ALG_SHA3_224 ((psa_algorithm_t)0x02000010)
#endif

#ifdef PSA_ALG_SHA3_256
#define SPEC_PSA_ALG_SHA3_256 ((psa_algorithm_t)0x02000011)
#else
#define PSA_ALG_SHA3_256 ((psa_algorithm_t)0x02000011)
#endif

#ifdef PSA_ALG_SHA3_384
#define SPEC_PSA_ALG_SHA3_384 ((psa_algorithm_t)0x02000012)
#else
#define PSA_ALG_SHA3_384 ((psa_algorithm_t)0x02000012)
#endif

#ifdef PSA_ALG_SHA3_512
#define SPEC_PSA_ALG_SHA3_512 ((psa_algorithm_t)0x02000013)
#else
#define PSA_ALG_SHA3_512 ((psa_algorithm_t)0x02000013)
#endif

#ifdef PSA_ALG_SHA_1
#define SPEC_PSA_ALG_SHA_1 ((psa_algorithm_t)0x02000005)
#else
#define PSA_ALG_SHA_1 ((psa_algorithm_t)0x02000005)
#endif

#ifdef PSA_ALG_SHA_224
#define SPEC_PSA_ALG_SHA_224 ((psa_algorithm_t)0x02000008)
#else
#define PSA_ALG_SHA_224 ((psa_algorithm_t)0x02000008)
#endif

#ifdef PSA_ALG_SHA_256
#define SPEC_PSA_ALG_SHA_256 ((psa_algorithm_t)0x02000009)
#else
#define PSA_ALG_SHA_256 ((psa_algorithm_t)0x02000009)
#endif

#ifdef PSA_ALG_SHA_384
#define SPEC_PSA_ALG_SHA_384 ((psa_algorithm_t)0x0200000a)
#else
#define PSA_ALG_SHA_384 ((psa_algorithm_t)0x0200000a)
#endif

#ifdef PSA_ALG_SHA_512
#define SPEC_PSA_ALG_SHA_512 ((psa_algorithm_t)0x0200000b)
#else
#define PSA_ALG_SHA_512 ((psa_algorithm_t)0x0200000b)
#endif

#ifdef PSA_ALG_SHA_512_224
#define SPEC_PSA_ALG_SHA_512_224 ((psa_algorithm_t)0x0200000c)
#else
#define PSA_ALG_SHA_512_224 ((psa_algorithm_t)0x0200000c)
#endif

#ifdef PSA_ALG_SHA_512_256
#define SPEC_PSA_ALG_SHA_512_256 ((psa_algorithm_t)0x0200000d)
#else
#define PSA_ALG_SHA_512_256 ((psa_algorithm_t)0x0200000d)
#endif

#ifdef PSA_ALG_SM3
#define SPEC_PSA_ALG_SM3 ((psa_algorithm_t)0x02000014)
#else
#define PSA_ALG_SM3 ((psa_algorithm_t)0x02000014)
#endif

#ifdef PSA_ALG_STREAM_CIPHER
#define SPEC_PSA_ALG_STREAM_CIPHER ((psa_algorithm_t)0x04800100)
#else
#define PSA_ALG_STREAM_CIPHER ((psa_algorithm_t)0x04800100)
#endif

#ifdef PSA_ALG_TLS12_PRF
#define SPEC_PSA_ALG_TLS12_PRF(hash_alg) \
        ((psa_algorithm_t) (0x08000200 | ((hash_alg) & 0x000000ff)))
#else
#define PSA_ALG_TLS12_PRF(hash_alg) \
        ((psa_algorithm_t) (0x08000200 | ((hash_alg) & 0x000000ff)))
#endif

#ifdef PSA_ALG_TLS12_PSK_TO_MS
#define SPEC_PSA_ALG_TLS12_PSK_TO_MS(hash_alg) \
        ((psa_algorithm_t) (0x08000300 | ((hash_alg) & 0x000000ff)))
#else
#define PSA_ALG_TLS12_PSK_TO_MS(hash_alg) \
        ((psa_algorithm_t) (0x08000300 | ((hash_alg) & 0x000000ff)))
#endif

#ifdef PSA_ALG_TRUNCATED_MAC
#define SPEC_PSA_ALG_TRUNCATED_MAC(mac_alg, mac_length) \
        ((psa_algorithm_t) (((mac_alg) & ~0x003f0000) | (((mac_length) & 0x3f) << 16)))
#else
#define PSA_ALG_TRUNCATED_MAC(mac_alg, mac_length) \
        ((psa_algorithm_t) (((mac_alg) & ~0x003f0000) | (((mac_length) & 0x3f) << 16)))
#endif

#ifdef PSA_ALG_XTS
#define SPEC_PSA_ALG_XTS ((psa_algorithm_t)0x0440ff00)
#else
#define PSA_ALG_XTS ((psa_algorithm_t)0x0440ff00)
#endif

#ifdef PSA_BLOCK_CIPHER_BLOCK_LENGTH
#define SPEC_PSA_BLOCK_CIPHER_BLOCK_LENGTH(type) \
        (1u << (((type) >> 8) & 7))
#else
#define PSA_BLOCK_CIPHER_BLOCK_LENGTH(type) \
        (1u << (((type) >> 8) & 7))
#endif

#ifdef PSA_CRYPTO_API_VERSION_MAJOR
#define SPEC_PSA_CRYPTO_API_VERSION_MAJOR 1
#else
#define PSA_CRYPTO_API_VERSION_MAJOR 1
#endif

#ifdef PSA_CRYPTO_API_VERSION_MINOR
#define SPEC_PSA_CRYPTO_API_VERSION_MINOR 0
#else
#define PSA_CRYPTO_API_VERSION_MINOR 0
#endif

#ifdef PSA_DH_FAMILY_RFC7919
#define SPEC_PSA_DH_FAMILY_RFC7919 ((psa_dh_family_t) 0x03)
#else
#define PSA_DH_FAMILY_RFC7919 ((psa_dh_family_t) 0x03)
#endif

#ifdef PSA_ECC_FAMILY_BRAINPOOL_P_R1
#define SPEC_PSA_ECC_FAMILY_BRAINPOOL_P_R1 ((psa_ecc_family_t) 0x30)
#else
#define PSA_ECC_FAMILY_BRAINPOOL_P_R1 ((psa_ecc_family_t) 0x30)
#endif

#ifdef PSA_ECC_FAMILY_FRP
#define SPEC_PSA_ECC_FAMILY_FRP ((psa_ecc_family_t) 0x33)
#else
#define PSA_ECC_FAMILY_FRP ((psa_ecc_family_t) 0x33)
#endif

#ifdef PSA_ECC_FAMILY_MONTGOMERY
#define SPEC_PSA_ECC_FAMILY_MONTGOMERY ((psa_ecc_family_t) 0x41)
#else
#define PSA_ECC_FAMILY_MONTGOMERY ((psa_ecc_family_t) 0x41)
#endif

#ifdef PSA_ECC_FAMILY_SECP_K1
#define SPEC_PSA_ECC_FAMILY_SECP_K1 ((psa_ecc_family_t) 0x17)
#else
#define PSA_ECC_FAMILY_SECP_K1 ((psa_ecc_family_t) 0x17)
#endif

#ifdef PSA_ECC_FAMILY_SECP_R1
#define SPEC_PSA_ECC_FAMILY_SECP_R1 ((psa_ecc_family_t) 0x12)
#else
#define PSA_ECC_FAMILY_SECP_R1 ((psa_ecc_family_t) 0x12)
#endif

#ifdef PSA_ECC_FAMILY_SECP_R2
#define SPEC_PSA_ECC_FAMILY_SECP_R2 ((psa_ecc_family_t) 0x1b)
#else
#define PSA_ECC_FAMILY_SECP_R2 ((psa_ecc_family_t) 0x1b)
#endif

#ifdef PSA_ECC_FAMILY_SECT_K1
#define SPEC_PSA_ECC_FAMILY_SECT_K1 ((psa_ecc_family_t) 0x27)
#else
#define PSA_ECC_FAMILY_SECT_K1 ((psa_ecc_family_t) 0x27)
#endif

#ifdef PSA_ECC_FAMILY_SECT_R1
#define SPEC_PSA_ECC_FAMILY_SECT_R1 ((psa_ecc_family_t) 0x22)
#else
#define PSA_ECC_FAMILY_SECT_R1 ((psa_ecc_family_t) 0x22)
#endif

#ifdef PSA_ECC_FAMILY_SECT_R2
#define SPEC_PSA_ECC_FAMILY_SECT_R2 ((psa_ecc_family_t) 0x2b)
#else
#define PSA_ECC_FAMILY_SECT_R2 ((psa_ecc_family_t) 0x2b)
#endif

#ifdef PSA_ERROR_ALREADY_EXISTS
#define SPEC_PSA_ERROR_ALREADY_EXISTS ((psa_status_t)-139)
#else
#define PSA_ERROR_ALREADY_EXISTS ((psa_status_t)-139)
#endif

#ifdef PSA_ERROR_BAD_STATE
#define SPEC_PSA_ERROR_BAD_STATE ((psa_status_t)-137)
#else
#define PSA_ERROR_BAD_STATE ((psa_status_t)-137)
#endif

#ifdef PSA_ERROR_BUFFER_TOO_SMALL
#define SPEC_PSA_ERROR_BUFFER_TOO_SMALL ((psa_status_t)-138)
#else
#define PSA_ERROR_BUFFER_TOO_SMALL ((psa_status_t)-138)
#endif

#ifdef PSA_ERROR_COMMUNICATION_FAILURE
#define SPEC_PSA_ERROR_COMMUNICATION_FAILURE ((psa_status_t)-145)
#else
#define PSA_ERROR_COMMUNICATION_FAILURE ((psa_status_t)-145)
#endif

#ifdef PSA_ERROR_CORRUPTION_DETECTED
#define SPEC_PSA_ERROR_CORRUPTION_DETECTED ((psa_status_t)-151)
#else
#define PSA_ERROR_CORRUPTION_DETECTED ((psa_status_t)-151)
#endif

#ifdef PSA_ERROR_DATA_CORRUPT
#define SPEC_PSA_ERROR_DATA_CORRUPT ((psa_status_t)-152)
#else
#define PSA_ERROR_DATA_CORRUPT ((psa_status_t)-152)
#endif

#ifdef PSA_ERROR_DATA_INVALID
#define SPEC_PSA_ERROR_DATA_INVALID ((psa_status_t)-153)
#else
#define PSA_ERROR_DATA_INVALID ((psa_status_t)-153)
#endif

#ifdef PSA_ERROR_DOES_NOT_EXIST
#define SPEC_PSA_ERROR_DOES_NOT_EXIST ((psa_status_t)-140)
#else
#define PSA_ERROR_DOES_NOT_EXIST ((psa_status_t)-140)
#endif

#ifdef PSA_ERROR_GENERIC_ERROR
#define SPEC_PSA_ERROR_GENERIC_ERROR ((psa_status_t)-132)
#else
#define PSA_ERROR_GENERIC_ERROR ((psa_status_t)-132)
#endif

#ifdef PSA_ERROR_HARDWARE_FAILURE
#define SPEC_PSA_ERROR_HARDWARE_FAILURE ((psa_status_t)-147)
#else
#define PSA_ERROR_HARDWARE_FAILURE ((psa_status_t)-147)
#endif

#ifdef PSA_ERROR_INSUFFICIENT_DATA
#define SPEC_PSA_ERROR_INSUFFICIENT_DATA ((psa_status_t)-143)
#else
#define PSA_ERROR_INSUFFICIENT_DATA ((psa_status_t)-143)
#endif

#ifdef PSA_ERROR_INSUFFICIENT_ENTROPY
#define SPEC_PSA_ERROR_INSUFFICIENT_ENTROPY ((psa_status_t)-148)
#else
#define PSA_ERROR_INSUFFICIENT_ENTROPY ((psa_status_t)-148)
#endif

#ifdef PSA_ERROR_INSUFFICIENT_MEMORY
#define SPEC_PSA_ERROR_INSUFFICIENT_MEMORY ((psa_status_t)-141)
#else
#define PSA_ERROR_INSUFFICIENT_MEMORY ((psa_status_t)-141)
#endif

#ifdef PSA_ERROR_INSUFFICIENT_STORAGE
#define SPEC_PSA_ERROR_INSUFFICIENT_STORAGE ((psa_status_t)-142)
#else
#define PSA_ERROR_INSUFFICIENT_STORAGE ((psa_status_t)-142)
#endif

#ifdef PSA_ERROR_INVALID_ARGUMENT
#define SPEC_PSA_ERROR_INVALID_ARGUMENT ((psa_status_t)-135)
#else
#define PSA_ERROR_INVALID_ARGUMENT ((psa_status_t)-135)
#endif

#ifdef PSA_ERROR_INVALID_HANDLE
#define SPEC_PSA_ERROR_INVALID_HANDLE ((psa_status_t)-136)
#else
#define PSA_ERROR_INVALID_HANDLE ((psa_status_t)-136)
#endif

#ifdef PSA_ERROR_INVALID_PADDING
#define SPEC_PSA_ERROR_INVALID_PADDING ((psa_status_t)-150)
#else
#define PSA_ERROR_INVALID_PADDING ((psa_status_t)-150)
#endif

#ifdef PSA_ERROR_INVALID_SIGNATURE
#define SPEC_PSA_ERROR_INVALID_SIGNATURE ((psa_status_t)-149)
#else
#define PSA_ERROR_INVALID_SIGNATURE ((psa_status_t)-149)
#endif

#ifdef PSA_ERROR_NOT_PERMITTED
#define SPEC_PSA_ERROR_NOT_PERMITTED ((psa_status_t)-133)
#else
#define PSA_ERROR_NOT_PERMITTED ((psa_status_t)-133)
#endif

#ifdef PSA_ERROR_NOT_SUPPORTED
#define SPEC_PSA_ERROR_NOT_SUPPORTED ((psa_status_t)-134)
#else
#define PSA_ERROR_NOT_SUPPORTED ((psa_status_t)-134)
#endif

#ifdef PSA_ERROR_STORAGE_FAILURE
#define SPEC_PSA_ERROR_STORAGE_FAILURE ((psa_status_t)-146)
#else
#define PSA_ERROR_STORAGE_FAILURE ((psa_status_t)-146)
#endif

#ifdef PSA_HASH_SUSPEND_ALGORITHM_FIELD_LENGTH
#define SPEC_PSA_HASH_SUSPEND_ALGORITHM_FIELD_LENGTH ((size_t)4)
#else
#define PSA_HASH_SUSPEND_ALGORITHM_FIELD_LENGTH ((size_t)4)
#endif

#ifdef PSA_HASH_SUSPEND_HASH_STATE_FIELD_LENGTH
#define SPEC_PSA_HASH_SUSPEND_HASH_STATE_FIELD_LENGTH(alg) \
        ((alg)==PSA_ALG_MD2 ? 64 : \
         (alg)==PSA_ALG_MD4 || (alg)==PSA_ALG_MD5 ? 16 : \
         (alg)==PSA_ALG_RIPEMD160 || (alg)==PSA_ALG_SHA_1 ? 20 : \
         (alg)==PSA_ALG_SHA_224 || (alg)==PSA_ALG_SHA_256 ? 32 : \
         (alg)==PSA_ALG_SHA_512 || (alg)==PSA_ALG_SHA_384 || (alg)==PSA_ALG_SHA_512_256 ? 64 : \
         0)
#else
#define PSA_HASH_SUSPEND_HASH_STATE_FIELD_LENGTH(alg) \
        ((alg)==PSA_ALG_MD2 ? 64 : \
         (alg)==PSA_ALG_MD4 || (alg)==PSA_ALG_MD5 ? 16 : \
         (alg)==PSA_ALG_RIPEMD160 || (alg)==PSA_ALG_SHA_1 ? 20 : \
         (alg)==PSA_ALG_SHA_224 || (alg)==PSA_ALG_SHA_256 ? 32 : \
         (alg)==PSA_ALG_SHA_512 || (alg)==PSA_ALG_SHA_384 || (alg)==PSA_ALG_SHA_512_256 ? 64 : \
         0)
#endif

#ifdef PSA_HASH_SUSPEND_INPUT_LENGTH_FIELD_LENGTH
#define SPEC_PSA_HASH_SUSPEND_INPUT_LENGTH_FIELD_LENGTH(alg) \
        ((alg)==PSA_ALG_MD2 ? 1 : \
         (alg)==PSA_ALG_MD4 || (alg)==PSA_ALG_MD5 || (alg)==PSA_ALG_RIPEMD160 || \
         (alg)==PSA_ALG_SHA_1 || (alg)==PSA_ALG_SHA_224 || (alg)==PSA_ALG_SHA_256 ? 8 : \
         (alg)==PSA_ALG_SHA_512 || (alg)==PSA_ALG_SHA_384 || (alg)==PSA_ALG_SHA_512_256 ? 16 : \
         0)
#else
#define PSA_HASH_SUSPEND_INPUT_LENGTH_FIELD_LENGTH(alg) \
        ((alg)==PSA_ALG_MD2 ? 1 : \
         (alg)==PSA_ALG_MD4 || (alg)==PSA_ALG_MD5 || (alg)==PSA_ALG_RIPEMD160 || \
         (alg)==PSA_ALG_SHA_1 || (alg)==PSA_ALG_SHA_224 || (alg)==PSA_ALG_SHA_256 ? 8 : \
         (alg)==PSA_ALG_SHA_512 || (alg)==PSA_ALG_SHA_384 || (alg)==PSA_ALG_SHA_512_256 ? 16 : \
         0)
#endif

#ifdef PSA_HASH_SUSPEND_OUTPUT_SIZE
#define SPEC_PSA_HASH_SUSPEND_OUTPUT_SIZE(alg) \
        (PSA_HASH_SUSPEND_ALGORITHM_FIELD_LENGTH + \
         PSA_HASH_SUSPEND_INPUT_LENGTH_FIELD_LENGTH(alg) + \
         PSA_HASH_SUSPEND_HASH_STATE_FIELD_LENGTH(alg) + \
         PSA_HASH_BLOCK_LENGTH(alg) - 1)
#else
#define PSA_HASH_SUSPEND_OUTPUT_SIZE(alg) \
        (PSA_HASH_SUSPEND_ALGORITHM_FIELD_LENGTH + \
         PSA_HASH_SUSPEND_INPUT_LENGTH_FIELD_LENGTH(alg) + \
         PSA_HASH_SUSPEND_HASH_STATE_FIELD_LENGTH(alg) + \
         PSA_HASH_BLOCK_LENGTH(alg) - 1)
#endif

#ifdef PSA_KEY_ID_NULL
#define SPEC_PSA_KEY_ID_NULL ((psa_key_id_t)0)
#else
#define PSA_KEY_ID_NULL ((psa_key_id_t)0)
#endif

#ifdef PSA_KEY_ID_USER_MAX
#define SPEC_PSA_KEY_ID_USER_MAX ((psa_key_id_t)0x3fffffff)
#else
#define PSA_KEY_ID_USER_MAX ((psa_key_id_t)0x3fffffff)
#endif

#ifdef PSA_KEY_ID_USER_MIN
#define SPEC_PSA_KEY_ID_USER_MIN ((psa_key_id_t)0x00000001)
#else
#define PSA_KEY_ID_USER_MIN ((psa_key_id_t)0x00000001)
#endif

#ifdef PSA_KEY_ID_VENDOR_MAX
#define SPEC_PSA_KEY_ID_VENDOR_MAX ((psa_key_id_t)0x7fffffff)
#else
#define PSA_KEY_ID_VENDOR_MAX ((psa_key_id_t)0x7fffffff)
#endif

#ifdef PSA_KEY_ID_VENDOR_MIN
#define SPEC_PSA_KEY_ID_VENDOR_MIN ((psa_key_id_t)0x40000000)
#else
#define PSA_KEY_ID_VENDOR_MIN ((psa_key_id_t)0x40000000)
#endif

#ifdef PSA_KEY_LIFETIME_PERSISTENT
#define SPEC_PSA_KEY_LIFETIME_PERSISTENT ((psa_key_lifetime_t)0x00000001)
#else
#define PSA_KEY_LIFETIME_PERSISTENT ((psa_key_lifetime_t)0x00000001)
#endif

#ifdef PSA_KEY_LIFETIME_VOLATILE
#define SPEC_PSA_KEY_LIFETIME_VOLATILE ((psa_key_lifetime_t)0x00000000)
#else
#define PSA_KEY_LIFETIME_VOLATILE ((psa_key_lifetime_t)0x00000000)
#endif

#ifdef PSA_KEY_TYPE_AES
#define SPEC_PSA_KEY_TYPE_AES ((psa_key_type_t)0x2400)
#else
#define PSA_KEY_TYPE_AES ((psa_key_type_t)0x2400)
#endif

#ifdef PSA_KEY_TYPE_ARC4
#define SPEC_PSA_KEY_TYPE_ARC4 ((psa_key_type_t)0x2002)
#else
#define PSA_KEY_TYPE_ARC4 ((psa_key_type_t)0x2002)
#endif

#ifdef PSA_KEY_TYPE_CAMELLIA
#define SPEC_PSA_KEY_TYPE_CAMELLIA ((psa_key_type_t)0x2403)
#else
#define PSA_KEY_TYPE_CAMELLIA ((psa_key_type_t)0x2403)
#endif

#ifdef PSA_KEY_TYPE_CHACHA20
#define SPEC_PSA_KEY_TYPE_CHACHA20 ((psa_key_type_t)0x2004)
#else
#define PSA_KEY_TYPE_CHACHA20 ((psa_key_type_t)0x2004)
#endif

#ifdef PSA_KEY_TYPE_DERIVE
#define SPEC_PSA_KEY_TYPE_DERIVE ((psa_key_type_t)0x1200)
#else
#define PSA_KEY_TYPE_DERIVE ((psa_key_type_t)0x1200)
#endif

#ifdef PSA_KEY_TYPE_DES
#define SPEC_PSA_KEY_TYPE_DES ((psa_key_type_t)0x2301)
#else
#define PSA_KEY_TYPE_DES ((psa_key_type_t)0x2301)
#endif

#ifdef PSA_KEY_TYPE_DH_KEY_PAIR
#define SPEC_PSA_KEY_TYPE_DH_KEY_PAIR(group) \
        ((psa_key_type_t) (0x7200 | (group)))
#else
#define PSA_KEY_TYPE_DH_KEY_PAIR(group) \
        ((psa_key_type_t) (0x7200 | (group)))
#endif

#ifdef PSA_KEY_TYPE_DH_PUBLIC_KEY
#define SPEC_PSA_KEY_TYPE_DH_PUBLIC_KEY(group) \
        ((psa_key_type_t) (0x4200 | (group)))
#else
#define PSA_KEY_TYPE_DH_PUBLIC_KEY(group) \
        ((psa_key_type_t) (0x4200 | (group)))
#endif

#ifdef PSA_KEY_TYPE_ECC_KEY_PAIR
#define SPEC_PSA_KEY_TYPE_ECC_KEY_PAIR(curve) \
        ((psa_key_type_t) (0x7100 | (curve)))
#else
#define PSA_KEY_TYPE_ECC_KEY_PAIR(curve) \
        ((psa_key_type_t) (0x7100 | (curve)))
#endif

#ifdef PSA_KEY_TYPE_ECC_PUBLIC_KEY
#define SPEC_PSA_KEY_TYPE_ECC_PUBLIC_KEY(curve) \
        ((psa_key_type_t) (0x4100 | (curve)))
#else
#define PSA_KEY_TYPE_ECC_PUBLIC_KEY(curve) \
        ((psa_key_type_t) (0x4100 | (curve)))
#endif

#ifdef PSA_KEY_TYPE_HMAC
#define SPEC_PSA_KEY_TYPE_HMAC ((psa_key_type_t)0x1100)
#else
#define PSA_KEY_TYPE_HMAC ((psa_key_type_t)0x1100)
#endif

#ifdef PSA_KEY_TYPE_KEY_PAIR_OF_PUBLIC_KEY
#define SPEC_PSA_KEY_TYPE_KEY_PAIR_OF_PUBLIC_KEY(type) \
        ((psa_key_type_t) ((type) | 0x3000))
#else
#define PSA_KEY_TYPE_KEY_PAIR_OF_PUBLIC_KEY(type) \
        ((psa_key_type_t) ((type) | 0x3000))
#endif

#ifdef PSA_KEY_TYPE_NONE
#define SPEC_PSA_KEY_TYPE_NONE ((psa_key_type_t)0x0000)
#else
#define PSA_KEY_TYPE_NONE ((psa_key_type_t)0x0000)
#endif

#ifdef PSA_KEY_TYPE_PUBLIC_KEY_OF_KEY_PAIR
#define SPEC_PSA_KEY_TYPE_PUBLIC_KEY_OF_KEY_PAIR(type) \
        ((psa_key_type_t) ((type) & ~0x3000))
#else
#define PSA_KEY_TYPE_PUBLIC_KEY_OF_KEY_PAIR(type) \
        ((psa_key_type_t) ((type) & ~0x3000))
#endif

#ifdef PSA_KEY_TYPE_RAW_DATA
#define SPEC_PSA_KEY_TYPE_RAW_DATA ((psa_key_type_t)0x1001)
#else
#define PSA_KEY_TYPE_RAW_DATA ((psa_key_type_t)0x1001)
#endif

#ifdef PSA_KEY_TYPE_RSA_KEY_PAIR
#define SPEC_PSA_KEY_TYPE_RSA_KEY_PAIR ((psa_key_type_t)0x7001)
#else
#define PSA_KEY_TYPE_RSA_KEY_PAIR ((psa_key_type_t)0x7001)
#endif

#ifdef PSA_KEY_TYPE_RSA_PUBLIC_KEY
#define SPEC_PSA_KEY_TYPE_RSA_PUBLIC_KEY ((psa_key_type_t)0x4001)
#else
#define PSA_KEY_TYPE_RSA_PUBLIC_KEY ((psa_key_type_t)0x4001)
#endif

#ifdef PSA_KEY_TYPE_SM4
#define SPEC_PSA_KEY_TYPE_SM4 ((psa_key_type_t)0x2405)
#else
#define PSA_KEY_TYPE_SM4 ((psa_key_type_t)0x2405)
#endif

#ifdef PSA_KEY_USAGE_CACHE
#define SPEC_PSA_KEY_USAGE_CACHE ((psa_key_usage_t)0x00000004)
#else
#define PSA_KEY_USAGE_CACHE ((psa_key_usage_t)0x00000004)
#endif

#ifdef PSA_KEY_USAGE_COPY
#define SPEC_PSA_KEY_USAGE_COPY ((psa_key_usage_t)0x00000002)
#else
#define PSA_KEY_USAGE_COPY ((psa_key_usage_t)0x00000002)
#endif

#ifdef PSA_KEY_USAGE_DECRYPT
#define SPEC_PSA_KEY_USAGE_DECRYPT ((psa_key_usage_t)0x00000200)
#else
#define PSA_KEY_USAGE_DECRYPT ((psa_key_usage_t)0x00000200)
#endif

#ifdef PSA_KEY_USAGE_DERIVE
#define SPEC_PSA_KEY_USAGE_DERIVE ((psa_key_usage_t)0x00004000)
#else
#define PSA_KEY_USAGE_DERIVE ((psa_key_usage_t)0x00004000)
#endif

#ifdef PSA_KEY_USAGE_ENCRYPT
#define SPEC_PSA_KEY_USAGE_ENCRYPT ((psa_key_usage_t)0x00000100)
#else
#define PSA_KEY_USAGE_ENCRYPT ((psa_key_usage_t)0x00000100)
#endif

#ifdef PSA_KEY_USAGE_EXPORT
#define SPEC_PSA_KEY_USAGE_EXPORT ((psa_key_usage_t)0x00000001)
#else
#define PSA_KEY_USAGE_EXPORT ((psa_key_usage_t)0x00000001)
#endif

#ifdef PSA_KEY_USAGE_SIGN_HASH
#define SPEC_PSA_KEY_USAGE_SIGN_HASH ((psa_key_usage_t)0x00001000)
#else
#define PSA_KEY_USAGE_SIGN_HASH ((psa_key_usage_t)0x00001000)
#endif

#ifdef PSA_KEY_USAGE_SIGN_MESSAGE
#define SPEC_PSA_KEY_USAGE_SIGN_MESSAGE ((psa_key_usage_t)0x00000400)
#else
#define PSA_KEY_USAGE_SIGN_MESSAGE ((psa_key_usage_t)0x00000400)
#endif

#ifdef PSA_KEY_USAGE_VERIFY_HASH
#define SPEC_PSA_KEY_USAGE_VERIFY_HASH ((psa_key_usage_t)0x00002000)
#else
#define PSA_KEY_USAGE_VERIFY_HASH ((psa_key_usage_t)0x00002000)
#endif

#ifdef PSA_KEY_USAGE_VERIFY_MESSAGE
#define SPEC_PSA_KEY_USAGE_VERIFY_MESSAGE ((psa_key_usage_t)0x00000800)
#else
#define PSA_KEY_USAGE_VERIFY_MESSAGE ((psa_key_usage_t)0x00000800)
#endif

#ifdef PSA_SUCCESS
#define SPEC_PSA_SUCCESS ((psa_status_t)0)
#else
#define PSA_SUCCESS ((psa_status_t)0)
#endif


#define hash_alg   PSA_ALG_SHA_256
#define mac_alg    PSA_ALG_CMAC
#define ka_alg     PSA_ALG_ECDH
#define kdf_alg    PSA_ALG_HKDF(PSA_ALG_SHA_256)
#define mac_length 4
#define group      0x2A
#define curve      0x2A
#define type       PSA_KEY_TYPE_RSA_PUBLIC_KEY

int main() {
    int ret = 0;

    #ifndef PSA_AEAD_DECRYPT_OUTPUT_MAX_SIZE
    printf("WARN:  %s is not defined\n", "PSA_AEAD_DECRYPT_OUTPUT_MAX_SIZE(ciphertext_length)");
#endif

#ifndef PSA_AEAD_DECRYPT_OUTPUT_SIZE
    printf("WARN:  %s is not defined\n", "PSA_AEAD_DECRYPT_OUTPUT_SIZE(key_type, alg, ciphertext_length)");
#endif

#ifndef PSA_AEAD_ENCRYPT_OUTPUT_MAX_SIZE
    printf("WARN:  %s is not defined\n", "PSA_AEAD_ENCRYPT_OUTPUT_MAX_SIZE(plaintext_length)");
#endif

#ifndef PSA_AEAD_ENCRYPT_OUTPUT_SIZE
    printf("WARN:  %s is not defined\n", "PSA_AEAD_ENCRYPT_OUTPUT_SIZE(key_type, alg, plaintext_length)");
#endif

#ifndef PSA_AEAD_FINISH_OUTPUT_MAX_SIZE
    printf("WARN:  %s is not defined\n", "PSA_AEAD_FINISH_OUTPUT_MAX_SIZE");
#endif

#ifndef PSA_AEAD_FINISH_OUTPUT_SIZE
    printf("WARN:  %s is not defined\n", "PSA_AEAD_FINISH_OUTPUT_SIZE(key_type, alg)");
#endif

#ifndef PSA_AEAD_NONCE_LENGTH
    printf("WARN:  %s is not defined\n", "PSA_AEAD_NONCE_LENGTH(key_type, alg)");
#endif

#ifndef PSA_AEAD_NONCE_MAX_SIZE
    printf("WARN:  %s is not defined\n", "PSA_AEAD_NONCE_MAX_SIZE");
#endif

#ifndef PSA_AEAD_OPERATION_INIT
    printf("WARN:  %s is not defined\n", "PSA_AEAD_OPERATION_INIT");
#endif

#ifndef PSA_AEAD_TAG_LENGTH
    printf("WARN:  %s is not defined\n", "PSA_AEAD_TAG_LENGTH(key_type, key_bits, alg)");
#endif

#ifndef PSA_AEAD_TAG_MAX_SIZE
    printf("WARN:  %s is not defined\n", "PSA_AEAD_TAG_MAX_SIZE");
#endif

#ifndef PSA_AEAD_UPDATE_OUTPUT_MAX_SIZE
    printf("WARN:  %s is not defined\n", "PSA_AEAD_UPDATE_OUTPUT_MAX_SIZE(input_length)");
#endif

#ifndef PSA_AEAD_UPDATE_OUTPUT_SIZE
    printf("WARN:  %s is not defined\n", "PSA_AEAD_UPDATE_OUTPUT_SIZE(key_type, alg, input_length)");
#endif

#ifndef PSA_AEAD_VERIFY_OUTPUT_MAX_SIZE
    printf("WARN:  %s is not defined\n", "PSA_AEAD_VERIFY_OUTPUT_MAX_SIZE");
#endif

#ifndef PSA_AEAD_VERIFY_OUTPUT_SIZE
    printf("WARN:  %s is not defined\n", "PSA_AEAD_VERIFY_OUTPUT_SIZE(key_type, alg)");
#endif

#ifdef SPEC_PSA_ALG_AEAD_WITH_DEFAULT_LENGTH_TAG
    if (PSA_ALG_AEAD_WITH_DEFAULT_LENGTH_TAG(aead_alg) != SPEC_PSA_ALG_AEAD_WITH_DEFAULT_LENGTH_TAG(aead_alg)) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_AEAD_WITH_DEFAULT_LENGTH_TAG(aead_alg)");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_AEAD_WITH_DEFAULT_LENGTH_TAG(aead_alg)");
#endif

#ifdef SPEC_PSA_ALG_AEAD_WITH_SHORTENED_TAG
    if (PSA_ALG_AEAD_WITH_SHORTENED_TAG(aead_alg, tag_length) != SPEC_PSA_ALG_AEAD_WITH_SHORTENED_TAG(aead_alg, tag_length)) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_AEAD_WITH_SHORTENED_TAG(aead_alg, tag_length)");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_AEAD_WITH_SHORTENED_TAG(aead_alg, tag_length)");
#endif

#ifdef SPEC_PSA_ALG_ANY_HASH
    if (PSA_ALG_ANY_HASH != SPEC_PSA_ALG_ANY_HASH) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_ANY_HASH");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_ANY_HASH");
#endif

#ifdef SPEC_PSA_ALG_CBC_MAC
    if (PSA_ALG_CBC_MAC != SPEC_PSA_ALG_CBC_MAC) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_CBC_MAC");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_CBC_MAC");
#endif

#ifdef SPEC_PSA_ALG_CBC_NO_PADDING
    if (PSA_ALG_CBC_NO_PADDING != SPEC_PSA_ALG_CBC_NO_PADDING) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_CBC_NO_PADDING");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_CBC_NO_PADDING");
#endif

#ifdef SPEC_PSA_ALG_CBC_PKCS7
    if (PSA_ALG_CBC_PKCS7 != SPEC_PSA_ALG_CBC_PKCS7) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_CBC_PKCS7");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_CBC_PKCS7");
#endif

#ifdef SPEC_PSA_ALG_CCM
    if (PSA_ALG_CCM != SPEC_PSA_ALG_CCM) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_CCM");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_CCM");
#endif

#ifdef SPEC_PSA_ALG_CFB
    if (PSA_ALG_CFB != SPEC_PSA_ALG_CFB) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_CFB");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_CFB");
#endif

#ifdef SPEC_PSA_ALG_CHACHA20_POLY1305
    if (PSA_ALG_CHACHA20_POLY1305 != SPEC_PSA_ALG_CHACHA20_POLY1305) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_CHACHA20_POLY1305");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_CHACHA20_POLY1305");
#endif

#ifdef SPEC_PSA_ALG_CMAC
    if (PSA_ALG_CMAC != SPEC_PSA_ALG_CMAC) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_CMAC");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_CMAC");
#endif

#ifdef SPEC_PSA_ALG_CTR
    if (PSA_ALG_CTR != SPEC_PSA_ALG_CTR) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_CTR");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_CTR");
#endif

#ifdef SPEC_PSA_ALG_DETERMINISTIC_ECDSA
    if (PSA_ALG_DETERMINISTIC_ECDSA(hash_alg) != SPEC_PSA_ALG_DETERMINISTIC_ECDSA(hash_alg)) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_DETERMINISTIC_ECDSA(hash_alg)");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_DETERMINISTIC_ECDSA(hash_alg)");
#endif

#ifdef SPEC_PSA_ALG_ECB_NO_PADDING
    if (PSA_ALG_ECB_NO_PADDING != SPEC_PSA_ALG_ECB_NO_PADDING) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_ECB_NO_PADDING");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_ECB_NO_PADDING");
#endif

#ifdef SPEC_PSA_ALG_ECDH
    if (PSA_ALG_ECDH != SPEC_PSA_ALG_ECDH) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_ECDH");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_ECDH");
#endif

#ifdef SPEC_PSA_ALG_ECDSA
    if (PSA_ALG_ECDSA(hash_alg) != SPEC_PSA_ALG_ECDSA(hash_alg)) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_ECDSA(hash_alg)");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_ECDSA(hash_alg)");
#endif

#ifdef SPEC_PSA_ALG_ECDSA_ANY
    if (PSA_ALG_ECDSA_ANY != SPEC_PSA_ALG_ECDSA_ANY) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_ECDSA_ANY");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_ECDSA_ANY");
#endif

#ifdef SPEC_PSA_ALG_FFDH
    if (PSA_ALG_FFDH != SPEC_PSA_ALG_FFDH) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_FFDH");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_FFDH");
#endif

#ifdef SPEC_PSA_ALG_FULL_LENGTH_MAC
    if (PSA_ALG_FULL_LENGTH_MAC(mac_alg) != SPEC_PSA_ALG_FULL_LENGTH_MAC(mac_alg)) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_FULL_LENGTH_MAC(mac_alg)");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_FULL_LENGTH_MAC(mac_alg)");
#endif

#ifdef SPEC_PSA_ALG_GCM
    if (PSA_ALG_GCM != SPEC_PSA_ALG_GCM) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_GCM");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_GCM");
#endif

#ifndef PSA_ALG_GET_HASH
    printf("WARN:  %s is not defined\n", "PSA_ALG_GET_HASH(alg)");
#endif

#ifdef SPEC_PSA_ALG_HKDF
    if (PSA_ALG_HKDF(hash_alg) != SPEC_PSA_ALG_HKDF(hash_alg)) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_HKDF(hash_alg)");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_HKDF(hash_alg)");
#endif

#ifdef SPEC_PSA_ALG_HMAC
    if (PSA_ALG_HMAC(hash_alg) != SPEC_PSA_ALG_HMAC(hash_alg)) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_HMAC(hash_alg)");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_HMAC(hash_alg)");
#endif

#ifndef PSA_ALG_IS_AEAD
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_AEAD(alg)");
#endif

#ifndef PSA_ALG_IS_AEAD_ON_BLOCK_CIPHER
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_AEAD_ON_BLOCK_CIPHER(alg)");
#endif

#ifndef PSA_ALG_IS_ASYMMETRIC_ENCRYPTION
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_ASYMMETRIC_ENCRYPTION(alg)");
#endif

#ifndef PSA_ALG_IS_BLOCK_CIPHER_MAC
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_BLOCK_CIPHER_MAC(alg)");
#endif

#ifndef PSA_ALG_IS_CIPHER
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_CIPHER(alg)");
#endif

#ifndef PSA_ALG_IS_DETERMINISTIC_ECDSA
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_DETERMINISTIC_ECDSA(alg)");
#endif

#ifndef PSA_ALG_IS_ECDH
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_ECDH(alg)");
#endif

#ifndef PSA_ALG_IS_ECDSA
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_ECDSA(alg)");
#endif

#ifndef PSA_ALG_IS_FFDH
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_FFDH(alg)");
#endif

#ifndef PSA_ALG_IS_HASH
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_HASH(alg)");
#endif

#ifndef PSA_ALG_IS_HASH_AND_SIGN
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_HASH_AND_SIGN(alg)");
#endif

#ifndef PSA_ALG_IS_HKDF
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_HKDF(alg)");
#endif

#ifndef PSA_ALG_IS_HMAC
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_HMAC(alg)");
#endif

#ifndef PSA_ALG_IS_KEY_AGREEMENT
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_KEY_AGREEMENT(alg)");
#endif

#ifndef PSA_ALG_IS_KEY_DERIVATION
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_KEY_DERIVATION(alg)");
#endif

#ifndef PSA_ALG_IS_MAC
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_MAC(alg)");
#endif

#ifndef PSA_ALG_IS_RANDOMIZED_ECDSA
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_RANDOMIZED_ECDSA(alg)");
#endif

#ifndef PSA_ALG_IS_RAW_KEY_AGREEMENT
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_RAW_KEY_AGREEMENT(alg)");
#endif

#ifndef PSA_ALG_IS_RSA_OAEP
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_RSA_OAEP(alg)");
#endif

#ifndef PSA_ALG_IS_RSA_PKCS1V15_SIGN
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_RSA_PKCS1V15_SIGN(alg)");
#endif

#ifndef PSA_ALG_IS_RSA_PSS
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_RSA_PSS(alg)");
#endif

#ifndef PSA_ALG_IS_SIGN
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_SIGN(alg)");
#endif

#ifndef PSA_ALG_IS_SIGN_HASH
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_SIGN_HASH(alg)");
#endif

#ifndef PSA_ALG_IS_SIGN_MESSAGE
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_SIGN_MESSAGE(alg)");
#endif

#ifndef PSA_ALG_IS_STREAM_CIPHER
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_STREAM_CIPHER(alg)");
#endif

#ifndef PSA_ALG_IS_TLS12_PRF
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_TLS12_PRF(alg)");
#endif

#ifndef PSA_ALG_IS_TLS12_PSK_TO_MS
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_TLS12_PSK_TO_MS(alg)");
#endif

#ifndef PSA_ALG_IS_WILDCARD
    printf("WARN:  %s is not defined\n", "PSA_ALG_IS_WILDCARD(alg)");
#endif

#ifdef SPEC_PSA_ALG_KEY_AGREEMENT
    if (PSA_ALG_KEY_AGREEMENT(ka_alg, kdf_alg) != SPEC_PSA_ALG_KEY_AGREEMENT(ka_alg, kdf_alg)) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_KEY_AGREEMENT(ka_alg, kdf_alg)");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_KEY_AGREEMENT(ka_alg, kdf_alg)");
#endif

#ifndef PSA_ALG_KEY_AGREEMENT_GET_BASE
    printf("WARN:  %s is not defined\n", "PSA_ALG_KEY_AGREEMENT_GET_BASE(alg)");
#endif

#ifndef PSA_ALG_KEY_AGREEMENT_GET_KDF
    printf("WARN:  %s is not defined\n", "PSA_ALG_KEY_AGREEMENT_GET_KDF(alg)");
#endif

#ifdef SPEC_PSA_ALG_MD2
    if (PSA_ALG_MD2 != SPEC_PSA_ALG_MD2) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_MD2");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_MD2");
#endif

#ifdef SPEC_PSA_ALG_MD4
    if (PSA_ALG_MD4 != SPEC_PSA_ALG_MD4) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_MD4");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_MD4");
#endif

#ifdef SPEC_PSA_ALG_MD5
    if (PSA_ALG_MD5 != SPEC_PSA_ALG_MD5) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_MD5");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_MD5");
#endif

#ifdef SPEC_PSA_ALG_NONE
    if (PSA_ALG_NONE != SPEC_PSA_ALG_NONE) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_NONE");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_NONE");
#endif

#ifdef SPEC_PSA_ALG_OFB
    if (PSA_ALG_OFB != SPEC_PSA_ALG_OFB) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_OFB");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_OFB");
#endif

#ifdef SPEC_PSA_ALG_RIPEMD160
    if (PSA_ALG_RIPEMD160 != SPEC_PSA_ALG_RIPEMD160) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_RIPEMD160");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_RIPEMD160");
#endif

#ifdef SPEC_PSA_ALG_RSA_OAEP
    if (PSA_ALG_RSA_OAEP(hash_alg) != SPEC_PSA_ALG_RSA_OAEP(hash_alg)) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_RSA_OAEP(hash_alg)");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_RSA_OAEP(hash_alg)");
#endif

#ifdef SPEC_PSA_ALG_RSA_PKCS1V15_CRYPT
    if (PSA_ALG_RSA_PKCS1V15_CRYPT != SPEC_PSA_ALG_RSA_PKCS1V15_CRYPT) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_RSA_PKCS1V15_CRYPT");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_RSA_PKCS1V15_CRYPT");
#endif

#ifdef SPEC_PSA_ALG_RSA_PKCS1V15_SIGN
    if (PSA_ALG_RSA_PKCS1V15_SIGN(hash_alg) != SPEC_PSA_ALG_RSA_PKCS1V15_SIGN(hash_alg)) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_RSA_PKCS1V15_SIGN(hash_alg)");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_RSA_PKCS1V15_SIGN(hash_alg)");
#endif

#ifdef SPEC_PSA_ALG_RSA_PKCS1V15_SIGN_RAW
    if (PSA_ALG_RSA_PKCS1V15_SIGN_RAW != SPEC_PSA_ALG_RSA_PKCS1V15_SIGN_RAW) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_RSA_PKCS1V15_SIGN_RAW");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_RSA_PKCS1V15_SIGN_RAW");
#endif

#ifdef SPEC_PSA_ALG_RSA_PSS
    if (PSA_ALG_RSA_PSS(hash_alg) != SPEC_PSA_ALG_RSA_PSS(hash_alg)) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_RSA_PSS(hash_alg)");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_RSA_PSS(hash_alg)");
#endif

#ifdef SPEC_PSA_ALG_SHA3_224
    if (PSA_ALG_SHA3_224 != SPEC_PSA_ALG_SHA3_224) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_SHA3_224");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_SHA3_224");
#endif

#ifdef SPEC_PSA_ALG_SHA3_256
    if (PSA_ALG_SHA3_256 != SPEC_PSA_ALG_SHA3_256) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_SHA3_256");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_SHA3_256");
#endif

#ifdef SPEC_PSA_ALG_SHA3_384
    if (PSA_ALG_SHA3_384 != SPEC_PSA_ALG_SHA3_384) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_SHA3_384");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_SHA3_384");
#endif

#ifdef SPEC_PSA_ALG_SHA3_512
    if (PSA_ALG_SHA3_512 != SPEC_PSA_ALG_SHA3_512) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_SHA3_512");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_SHA3_512");
#endif

#ifdef SPEC_PSA_ALG_SHA_1
    if (PSA_ALG_SHA_1 != SPEC_PSA_ALG_SHA_1) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_SHA_1");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_SHA_1");
#endif

#ifdef SPEC_PSA_ALG_SHA_224
    if (PSA_ALG_SHA_224 != SPEC_PSA_ALG_SHA_224) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_SHA_224");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_SHA_224");
#endif

#ifdef SPEC_PSA_ALG_SHA_256
    if (PSA_ALG_SHA_256 != SPEC_PSA_ALG_SHA_256) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_SHA_256");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_SHA_256");
#endif

#ifdef SPEC_PSA_ALG_SHA_384
    if (PSA_ALG_SHA_384 != SPEC_PSA_ALG_SHA_384) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_SHA_384");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_SHA_384");
#endif

#ifdef SPEC_PSA_ALG_SHA_512
    if (PSA_ALG_SHA_512 != SPEC_PSA_ALG_SHA_512) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_SHA_512");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_SHA_512");
#endif

#ifdef SPEC_PSA_ALG_SHA_512_224
    if (PSA_ALG_SHA_512_224 != SPEC_PSA_ALG_SHA_512_224) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_SHA_512_224");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_SHA_512_224");
#endif

#ifdef SPEC_PSA_ALG_SHA_512_256
    if (PSA_ALG_SHA_512_256 != SPEC_PSA_ALG_SHA_512_256) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_SHA_512_256");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_SHA_512_256");
#endif

#ifdef SPEC_PSA_ALG_SM3
    if (PSA_ALG_SM3 != SPEC_PSA_ALG_SM3) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_SM3");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_SM3");
#endif

#ifdef SPEC_PSA_ALG_STREAM_CIPHER
    if (PSA_ALG_STREAM_CIPHER != SPEC_PSA_ALG_STREAM_CIPHER) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_STREAM_CIPHER");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_STREAM_CIPHER");
#endif

#ifdef SPEC_PSA_ALG_TLS12_PRF
    if (PSA_ALG_TLS12_PRF(hash_alg) != SPEC_PSA_ALG_TLS12_PRF(hash_alg)) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_TLS12_PRF(hash_alg)");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_TLS12_PRF(hash_alg)");
#endif

#ifdef SPEC_PSA_ALG_TLS12_PSK_TO_MS
    if (PSA_ALG_TLS12_PSK_TO_MS(hash_alg) != SPEC_PSA_ALG_TLS12_PSK_TO_MS(hash_alg)) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_TLS12_PSK_TO_MS(hash_alg)");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_TLS12_PSK_TO_MS(hash_alg)");
#endif

#ifdef SPEC_PSA_ALG_TRUNCATED_MAC
    if (PSA_ALG_TRUNCATED_MAC(mac_alg, mac_length) != SPEC_PSA_ALG_TRUNCATED_MAC(mac_alg, mac_length)) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_TRUNCATED_MAC(mac_alg, mac_length)");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_TRUNCATED_MAC(mac_alg, mac_length)");
#endif

#ifdef SPEC_PSA_ALG_XTS
    if (PSA_ALG_XTS != SPEC_PSA_ALG_XTS) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ALG_XTS");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ALG_XTS");
#endif

#ifndef PSA_ASYMMETRIC_DECRYPT_OUTPUT_MAX_SIZE
    printf("WARN:  %s is not defined\n", "PSA_ASYMMETRIC_DECRYPT_OUTPUT_MAX_SIZE");
#endif

#ifndef PSA_ASYMMETRIC_DECRYPT_OUTPUT_SIZE
    printf("WARN:  %s is not defined\n", "PSA_ASYMMETRIC_DECRYPT_OUTPUT_SIZE(key_type, key_bits, alg)");
#endif

#ifndef PSA_ASYMMETRIC_ENCRYPT_OUTPUT_MAX_SIZE
    printf("WARN:  %s is not defined\n", "PSA_ASYMMETRIC_ENCRYPT_OUTPUT_MAX_SIZE");
#endif

#ifndef PSA_ASYMMETRIC_ENCRYPT_OUTPUT_SIZE
    printf("WARN:  %s is not defined\n", "PSA_ASYMMETRIC_ENCRYPT_OUTPUT_SIZE(key_type, key_bits, alg)");
#endif

#ifdef SPEC_PSA_BLOCK_CIPHER_BLOCK_LENGTH
    if (PSA_BLOCK_CIPHER_BLOCK_LENGTH(type) != SPEC_PSA_BLOCK_CIPHER_BLOCK_LENGTH(type)) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_BLOCK_CIPHER_BLOCK_LENGTH(type)");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_BLOCK_CIPHER_BLOCK_LENGTH(type)");
#endif

#ifndef PSA_BLOCK_CIPHER_BLOCK_MAX_SIZE
    printf("WARN:  %s is not defined\n", "PSA_BLOCK_CIPHER_BLOCK_MAX_SIZE");
#endif

#ifndef PSA_CIPHER_DECRYPT_OUTPUT_MAX_SIZE
    printf("WARN:  %s is not defined\n", "PSA_CIPHER_DECRYPT_OUTPUT_MAX_SIZE(input_length)");
#endif

#ifndef PSA_CIPHER_DECRYPT_OUTPUT_SIZE
    printf("WARN:  %s is not defined\n", "PSA_CIPHER_DECRYPT_OUTPUT_SIZE(key_type, alg, input_length)");
#endif

#ifndef PSA_CIPHER_ENCRYPT_OUTPUT_MAX_SIZE
    printf("WARN:  %s is not defined\n", "PSA_CIPHER_ENCRYPT_OUTPUT_MAX_SIZE(input_length)");
#endif

#ifndef PSA_CIPHER_ENCRYPT_OUTPUT_SIZE
    printf("WARN:  %s is not defined\n", "PSA_CIPHER_ENCRYPT_OUTPUT_SIZE(key_type, alg, input_length)");
#endif

#ifndef PSA_CIPHER_FINISH_OUTPUT_MAX_SIZE
    printf("WARN:  %s is not defined\n", "PSA_CIPHER_FINISH_OUTPUT_MAX_SIZE");
#endif

#ifndef PSA_CIPHER_FINISH_OUTPUT_SIZE
    printf("WARN:  %s is not defined\n", "PSA_CIPHER_FINISH_OUTPUT_SIZE(key_type, alg)");
#endif

#ifndef PSA_CIPHER_IV_LENGTH
    printf("WARN:  %s is not defined\n", "PSA_CIPHER_IV_LENGTH(key_type, alg)");
#endif

#ifndef PSA_CIPHER_IV_MAX_SIZE
    printf("WARN:  %s is not defined\n", "PSA_CIPHER_IV_MAX_SIZE");
#endif

#ifndef PSA_CIPHER_OPERATION_INIT
    printf("WARN:  %s is not defined\n", "PSA_CIPHER_OPERATION_INIT");
#endif

#ifndef PSA_CIPHER_UPDATE_OUTPUT_MAX_SIZE
    printf("WARN:  %s is not defined\n", "PSA_CIPHER_UPDATE_OUTPUT_MAX_SIZE(input_length)");
#endif

#ifndef PSA_CIPHER_UPDATE_OUTPUT_SIZE
    printf("WARN:  %s is not defined\n", "PSA_CIPHER_UPDATE_OUTPUT_SIZE(key_type, alg, input_length)");
#endif

#ifdef SPEC_PSA_CRYPTO_API_VERSION_MAJOR
    if (PSA_CRYPTO_API_VERSION_MAJOR != SPEC_PSA_CRYPTO_API_VERSION_MAJOR) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_CRYPTO_API_VERSION_MAJOR");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_CRYPTO_API_VERSION_MAJOR");
#endif

#ifdef SPEC_PSA_CRYPTO_API_VERSION_MINOR
    if (PSA_CRYPTO_API_VERSION_MINOR != SPEC_PSA_CRYPTO_API_VERSION_MINOR) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_CRYPTO_API_VERSION_MINOR");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_CRYPTO_API_VERSION_MINOR");
#endif

#ifdef SPEC_PSA_DH_FAMILY_RFC7919
    if (PSA_DH_FAMILY_RFC7919 != SPEC_PSA_DH_FAMILY_RFC7919) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_DH_FAMILY_RFC7919");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_DH_FAMILY_RFC7919");
#endif

#ifdef SPEC_PSA_ECC_FAMILY_BRAINPOOL_P_R1
    if (PSA_ECC_FAMILY_BRAINPOOL_P_R1 != SPEC_PSA_ECC_FAMILY_BRAINPOOL_P_R1) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ECC_FAMILY_BRAINPOOL_P_R1");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ECC_FAMILY_BRAINPOOL_P_R1");
#endif

#ifdef SPEC_PSA_ECC_FAMILY_FRP
    if (PSA_ECC_FAMILY_FRP != SPEC_PSA_ECC_FAMILY_FRP) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ECC_FAMILY_FRP");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ECC_FAMILY_FRP");
#endif

#ifdef SPEC_PSA_ECC_FAMILY_MONTGOMERY
    if (PSA_ECC_FAMILY_MONTGOMERY != SPEC_PSA_ECC_FAMILY_MONTGOMERY) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ECC_FAMILY_MONTGOMERY");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ECC_FAMILY_MONTGOMERY");
#endif

#ifdef SPEC_PSA_ECC_FAMILY_SECP_K1
    if (PSA_ECC_FAMILY_SECP_K1 != SPEC_PSA_ECC_FAMILY_SECP_K1) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ECC_FAMILY_SECP_K1");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ECC_FAMILY_SECP_K1");
#endif

#ifdef SPEC_PSA_ECC_FAMILY_SECP_R1
    if (PSA_ECC_FAMILY_SECP_R1 != SPEC_PSA_ECC_FAMILY_SECP_R1) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ECC_FAMILY_SECP_R1");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ECC_FAMILY_SECP_R1");
#endif

#ifdef SPEC_PSA_ECC_FAMILY_SECP_R2
    if (PSA_ECC_FAMILY_SECP_R2 != SPEC_PSA_ECC_FAMILY_SECP_R2) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ECC_FAMILY_SECP_R2");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ECC_FAMILY_SECP_R2");
#endif

#ifdef SPEC_PSA_ECC_FAMILY_SECT_K1
    if (PSA_ECC_FAMILY_SECT_K1 != SPEC_PSA_ECC_FAMILY_SECT_K1) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ECC_FAMILY_SECT_K1");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ECC_FAMILY_SECT_K1");
#endif

#ifdef SPEC_PSA_ECC_FAMILY_SECT_R1
    if (PSA_ECC_FAMILY_SECT_R1 != SPEC_PSA_ECC_FAMILY_SECT_R1) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ECC_FAMILY_SECT_R1");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ECC_FAMILY_SECT_R1");
#endif

#ifdef SPEC_PSA_ECC_FAMILY_SECT_R2
    if (PSA_ECC_FAMILY_SECT_R2 != SPEC_PSA_ECC_FAMILY_SECT_R2) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ECC_FAMILY_SECT_R2");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ECC_FAMILY_SECT_R2");
#endif

#ifdef SPEC_PSA_ERROR_ALREADY_EXISTS
    if (PSA_ERROR_ALREADY_EXISTS != SPEC_PSA_ERROR_ALREADY_EXISTS) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ERROR_ALREADY_EXISTS");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ERROR_ALREADY_EXISTS");
#endif

#ifdef SPEC_PSA_ERROR_BAD_STATE
    if (PSA_ERROR_BAD_STATE != SPEC_PSA_ERROR_BAD_STATE) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ERROR_BAD_STATE");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ERROR_BAD_STATE");
#endif

#ifdef SPEC_PSA_ERROR_BUFFER_TOO_SMALL
    if (PSA_ERROR_BUFFER_TOO_SMALL != SPEC_PSA_ERROR_BUFFER_TOO_SMALL) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ERROR_BUFFER_TOO_SMALL");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ERROR_BUFFER_TOO_SMALL");
#endif

#ifdef SPEC_PSA_ERROR_COMMUNICATION_FAILURE
    if (PSA_ERROR_COMMUNICATION_FAILURE != SPEC_PSA_ERROR_COMMUNICATION_FAILURE) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ERROR_COMMUNICATION_FAILURE");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ERROR_COMMUNICATION_FAILURE");
#endif

#ifdef SPEC_PSA_ERROR_CORRUPTION_DETECTED
    if (PSA_ERROR_CORRUPTION_DETECTED != SPEC_PSA_ERROR_CORRUPTION_DETECTED) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ERROR_CORRUPTION_DETECTED");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ERROR_CORRUPTION_DETECTED");
#endif

#ifdef SPEC_PSA_ERROR_DATA_CORRUPT
    if (PSA_ERROR_DATA_CORRUPT != SPEC_PSA_ERROR_DATA_CORRUPT) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ERROR_DATA_CORRUPT");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ERROR_DATA_CORRUPT");
#endif

#ifdef SPEC_PSA_ERROR_DATA_INVALID
    if (PSA_ERROR_DATA_INVALID != SPEC_PSA_ERROR_DATA_INVALID) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ERROR_DATA_INVALID");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ERROR_DATA_INVALID");
#endif

#ifdef SPEC_PSA_ERROR_DOES_NOT_EXIST
    if (PSA_ERROR_DOES_NOT_EXIST != SPEC_PSA_ERROR_DOES_NOT_EXIST) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ERROR_DOES_NOT_EXIST");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ERROR_DOES_NOT_EXIST");
#endif

#ifdef SPEC_PSA_ERROR_GENERIC_ERROR
    if (PSA_ERROR_GENERIC_ERROR != SPEC_PSA_ERROR_GENERIC_ERROR) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ERROR_GENERIC_ERROR");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ERROR_GENERIC_ERROR");
#endif

#ifdef SPEC_PSA_ERROR_HARDWARE_FAILURE
    if (PSA_ERROR_HARDWARE_FAILURE != SPEC_PSA_ERROR_HARDWARE_FAILURE) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ERROR_HARDWARE_FAILURE");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ERROR_HARDWARE_FAILURE");
#endif

#ifdef SPEC_PSA_ERROR_INSUFFICIENT_DATA
    if (PSA_ERROR_INSUFFICIENT_DATA != SPEC_PSA_ERROR_INSUFFICIENT_DATA) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ERROR_INSUFFICIENT_DATA");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ERROR_INSUFFICIENT_DATA");
#endif

#ifdef SPEC_PSA_ERROR_INSUFFICIENT_ENTROPY
    if (PSA_ERROR_INSUFFICIENT_ENTROPY != SPEC_PSA_ERROR_INSUFFICIENT_ENTROPY) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ERROR_INSUFFICIENT_ENTROPY");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ERROR_INSUFFICIENT_ENTROPY");
#endif

#ifdef SPEC_PSA_ERROR_INSUFFICIENT_MEMORY
    if (PSA_ERROR_INSUFFICIENT_MEMORY != SPEC_PSA_ERROR_INSUFFICIENT_MEMORY) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ERROR_INSUFFICIENT_MEMORY");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ERROR_INSUFFICIENT_MEMORY");
#endif

#ifdef SPEC_PSA_ERROR_INSUFFICIENT_STORAGE
    if (PSA_ERROR_INSUFFICIENT_STORAGE != SPEC_PSA_ERROR_INSUFFICIENT_STORAGE) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ERROR_INSUFFICIENT_STORAGE");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ERROR_INSUFFICIENT_STORAGE");
#endif

#ifdef SPEC_PSA_ERROR_INVALID_ARGUMENT
    if (PSA_ERROR_INVALID_ARGUMENT != SPEC_PSA_ERROR_INVALID_ARGUMENT) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ERROR_INVALID_ARGUMENT");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ERROR_INVALID_ARGUMENT");
#endif

#ifdef SPEC_PSA_ERROR_INVALID_HANDLE
    if (PSA_ERROR_INVALID_HANDLE != SPEC_PSA_ERROR_INVALID_HANDLE) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ERROR_INVALID_HANDLE");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ERROR_INVALID_HANDLE");
#endif

#ifdef SPEC_PSA_ERROR_INVALID_PADDING
    if (PSA_ERROR_INVALID_PADDING != SPEC_PSA_ERROR_INVALID_PADDING) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ERROR_INVALID_PADDING");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ERROR_INVALID_PADDING");
#endif

#ifdef SPEC_PSA_ERROR_INVALID_SIGNATURE
    if (PSA_ERROR_INVALID_SIGNATURE != SPEC_PSA_ERROR_INVALID_SIGNATURE) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ERROR_INVALID_SIGNATURE");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ERROR_INVALID_SIGNATURE");
#endif

#ifdef SPEC_PSA_ERROR_NOT_PERMITTED
    if (PSA_ERROR_NOT_PERMITTED != SPEC_PSA_ERROR_NOT_PERMITTED) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ERROR_NOT_PERMITTED");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ERROR_NOT_PERMITTED");
#endif

#ifdef SPEC_PSA_ERROR_NOT_SUPPORTED
    if (PSA_ERROR_NOT_SUPPORTED != SPEC_PSA_ERROR_NOT_SUPPORTED) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ERROR_NOT_SUPPORTED");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ERROR_NOT_SUPPORTED");
#endif

#ifdef SPEC_PSA_ERROR_STORAGE_FAILURE
    if (PSA_ERROR_STORAGE_FAILURE != SPEC_PSA_ERROR_STORAGE_FAILURE) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_ERROR_STORAGE_FAILURE");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_ERROR_STORAGE_FAILURE");
#endif

#ifndef PSA_EXPORT_KEY_OUTPUT_SIZE
    printf("WARN:  %s is not defined\n", "PSA_EXPORT_KEY_OUTPUT_SIZE(key_type, key_bits)");
#endif

#ifndef PSA_EXPORT_KEY_PAIR_MAX_SIZE
    printf("WARN:  %s is not defined\n", "PSA_EXPORT_KEY_PAIR_MAX_SIZE");
#endif

#ifndef PSA_EXPORT_PUBLIC_KEY_MAX_SIZE
    printf("WARN:  %s is not defined\n", "PSA_EXPORT_PUBLIC_KEY_MAX_SIZE");
#endif

#ifndef PSA_EXPORT_PUBLIC_KEY_OUTPUT_SIZE
    printf("WARN:  %s is not defined\n", "PSA_EXPORT_PUBLIC_KEY_OUTPUT_SIZE(key_type, key_bits)");
#endif

#ifndef PSA_HASH_BLOCK_LENGTH
    printf("WARN:  %s is not defined\n", "PSA_HASH_BLOCK_LENGTH(alg)");
#endif

#ifndef PSA_HASH_LENGTH
    printf("WARN:  %s is not defined\n", "PSA_HASH_LENGTH(alg)");
#endif

#ifndef PSA_HASH_MAX_SIZE
    printf("WARN:  %s is not defined\n", "PSA_HASH_MAX_SIZE");
#endif

#ifndef PSA_HASH_OPERATION_INIT
    printf("WARN:  %s is not defined\n", "PSA_HASH_OPERATION_INIT");
#endif

#ifdef SPEC_PSA_HASH_SUSPEND_ALGORITHM_FIELD_LENGTH
    if (PSA_HASH_SUSPEND_ALGORITHM_FIELD_LENGTH != SPEC_PSA_HASH_SUSPEND_ALGORITHM_FIELD_LENGTH) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_HASH_SUSPEND_ALGORITHM_FIELD_LENGTH");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_HASH_SUSPEND_ALGORITHM_FIELD_LENGTH");
#endif

#ifdef SPEC_PSA_HASH_SUSPEND_HASH_STATE_FIELD_LENGTH
    if (PSA_HASH_SUSPEND_HASH_STATE_FIELD_LENGTH(alg) != SPEC_PSA_HASH_SUSPEND_HASH_STATE_FIELD_LENGTH(alg)) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_HASH_SUSPEND_HASH_STATE_FIELD_LENGTH(alg)");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_HASH_SUSPEND_HASH_STATE_FIELD_LENGTH(alg)");
#endif

#ifdef SPEC_PSA_HASH_SUSPEND_INPUT_LENGTH_FIELD_LENGTH
    if (PSA_HASH_SUSPEND_INPUT_LENGTH_FIELD_LENGTH(alg) != SPEC_PSA_HASH_SUSPEND_INPUT_LENGTH_FIELD_LENGTH(alg)) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_HASH_SUSPEND_INPUT_LENGTH_FIELD_LENGTH(alg)");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_HASH_SUSPEND_INPUT_LENGTH_FIELD_LENGTH(alg)");
#endif

#ifndef PSA_HASH_SUSPEND_OUTPUT_MAX_SIZE
    printf("WARN:  %s is not defined\n", "PSA_HASH_SUSPEND_OUTPUT_MAX_SIZE");
#endif

#ifdef SPEC_PSA_HASH_SUSPEND_OUTPUT_SIZE
    if (PSA_HASH_SUSPEND_OUTPUT_SIZE(alg) != SPEC_PSA_HASH_SUSPEND_OUTPUT_SIZE(alg)) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_HASH_SUSPEND_OUTPUT_SIZE(alg)");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_HASH_SUSPEND_OUTPUT_SIZE(alg)");
#endif

#ifndef PSA_KEY_ATTRIBUTES_INIT
    printf("WARN:  %s is not defined\n", "PSA_KEY_ATTRIBUTES_INIT");
#endif

#ifndef PSA_KEY_DERIVATION_INPUT_CONTEXT
    printf("WARN:  %s is not defined\n", "PSA_KEY_DERIVATION_INPUT_CONTEXT");
#endif

#ifndef PSA_KEY_DERIVATION_INPUT_INFO
    printf("WARN:  %s is not defined\n", "PSA_KEY_DERIVATION_INPUT_INFO");
#endif

#ifndef PSA_KEY_DERIVATION_INPUT_LABEL
    printf("WARN:  %s is not defined\n", "PSA_KEY_DERIVATION_INPUT_LABEL");
#endif

#ifndef PSA_KEY_DERIVATION_INPUT_SALT
    printf("WARN:  %s is not defined\n", "PSA_KEY_DERIVATION_INPUT_SALT");
#endif

#ifndef PSA_KEY_DERIVATION_INPUT_SECRET
    printf("WARN:  %s is not defined\n", "PSA_KEY_DERIVATION_INPUT_SECRET");
#endif

#ifndef PSA_KEY_DERIVATION_INPUT_SEED
    printf("WARN:  %s is not defined\n", "PSA_KEY_DERIVATION_INPUT_SEED");
#endif

#ifndef PSA_KEY_DERIVATION_OPERATION_INIT
    printf("WARN:  %s is not defined\n", "PSA_KEY_DERIVATION_OPERATION_INIT");
#endif

#ifndef PSA_KEY_DERIVATION_UNLIMITED_CAPACITY
    printf("WARN:  %s is not defined\n", "PSA_KEY_DERIVATION_UNLIMITED_CAPACITY");
#endif

#ifdef SPEC_PSA_KEY_ID_NULL
    if (PSA_KEY_ID_NULL != SPEC_PSA_KEY_ID_NULL) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_ID_NULL");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_ID_NULL");
#endif

#ifdef SPEC_PSA_KEY_ID_USER_MAX
    if (PSA_KEY_ID_USER_MAX != SPEC_PSA_KEY_ID_USER_MAX) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_ID_USER_MAX");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_ID_USER_MAX");
#endif

#ifdef SPEC_PSA_KEY_ID_USER_MIN
    if (PSA_KEY_ID_USER_MIN != SPEC_PSA_KEY_ID_USER_MIN) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_ID_USER_MIN");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_ID_USER_MIN");
#endif

#ifdef SPEC_PSA_KEY_ID_VENDOR_MAX
    if (PSA_KEY_ID_VENDOR_MAX != SPEC_PSA_KEY_ID_VENDOR_MAX) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_ID_VENDOR_MAX");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_ID_VENDOR_MAX");
#endif

#ifdef SPEC_PSA_KEY_ID_VENDOR_MIN
    if (PSA_KEY_ID_VENDOR_MIN != SPEC_PSA_KEY_ID_VENDOR_MIN) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_ID_VENDOR_MIN");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_ID_VENDOR_MIN");
#endif

#ifdef SPEC_PSA_KEY_LIFETIME_PERSISTENT
    if (PSA_KEY_LIFETIME_PERSISTENT != SPEC_PSA_KEY_LIFETIME_PERSISTENT) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_LIFETIME_PERSISTENT");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_LIFETIME_PERSISTENT");
#endif

#ifdef SPEC_PSA_KEY_LIFETIME_VOLATILE
    if (PSA_KEY_LIFETIME_VOLATILE != SPEC_PSA_KEY_LIFETIME_VOLATILE) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_LIFETIME_VOLATILE");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_LIFETIME_VOLATILE");
#endif

#ifdef SPEC_PSA_KEY_TYPE_AES
    if (PSA_KEY_TYPE_AES != SPEC_PSA_KEY_TYPE_AES) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_TYPE_AES");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_AES");
#endif

#ifdef SPEC_PSA_KEY_TYPE_ARC4
    if (PSA_KEY_TYPE_ARC4 != SPEC_PSA_KEY_TYPE_ARC4) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_TYPE_ARC4");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_ARC4");
#endif

#ifdef SPEC_PSA_KEY_TYPE_CAMELLIA
    if (PSA_KEY_TYPE_CAMELLIA != SPEC_PSA_KEY_TYPE_CAMELLIA) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_TYPE_CAMELLIA");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_CAMELLIA");
#endif

#ifdef SPEC_PSA_KEY_TYPE_CHACHA20
    if (PSA_KEY_TYPE_CHACHA20 != SPEC_PSA_KEY_TYPE_CHACHA20) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_TYPE_CHACHA20");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_CHACHA20");
#endif

#ifdef SPEC_PSA_KEY_TYPE_DERIVE
    if (PSA_KEY_TYPE_DERIVE != SPEC_PSA_KEY_TYPE_DERIVE) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_TYPE_DERIVE");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_DERIVE");
#endif

#ifdef SPEC_PSA_KEY_TYPE_DES
    if (PSA_KEY_TYPE_DES != SPEC_PSA_KEY_TYPE_DES) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_TYPE_DES");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_DES");
#endif

#ifndef PSA_KEY_TYPE_DH_GET_FAMILY
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_DH_GET_FAMILY(type)");
#endif

#ifdef SPEC_PSA_KEY_TYPE_DH_KEY_PAIR
    if (PSA_KEY_TYPE_DH_KEY_PAIR(group) != SPEC_PSA_KEY_TYPE_DH_KEY_PAIR(group)) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_TYPE_DH_KEY_PAIR(group)");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_DH_KEY_PAIR(group)");
#endif

#ifdef SPEC_PSA_KEY_TYPE_DH_PUBLIC_KEY
    if (PSA_KEY_TYPE_DH_PUBLIC_KEY(group) != SPEC_PSA_KEY_TYPE_DH_PUBLIC_KEY(group)) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_TYPE_DH_PUBLIC_KEY(group)");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_DH_PUBLIC_KEY(group)");
#endif

#ifndef PSA_KEY_TYPE_ECC_GET_FAMILY
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_ECC_GET_FAMILY(type)");
#endif

#ifdef SPEC_PSA_KEY_TYPE_ECC_KEY_PAIR
    if (PSA_KEY_TYPE_ECC_KEY_PAIR(curve) != SPEC_PSA_KEY_TYPE_ECC_KEY_PAIR(curve)) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_TYPE_ECC_KEY_PAIR(curve)");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_ECC_KEY_PAIR(curve)");
#endif

#ifdef SPEC_PSA_KEY_TYPE_ECC_PUBLIC_KEY
    if (PSA_KEY_TYPE_ECC_PUBLIC_KEY(curve) != SPEC_PSA_KEY_TYPE_ECC_PUBLIC_KEY(curve)) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_TYPE_ECC_PUBLIC_KEY(curve)");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_ECC_PUBLIC_KEY(curve)");
#endif

#ifdef SPEC_PSA_KEY_TYPE_HMAC
    if (PSA_KEY_TYPE_HMAC != SPEC_PSA_KEY_TYPE_HMAC) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_TYPE_HMAC");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_HMAC");
#endif

#ifndef PSA_KEY_TYPE_IS_ASYMMETRIC
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_IS_ASYMMETRIC(type)");
#endif

#ifndef PSA_KEY_TYPE_IS_DH
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_IS_DH(type)");
#endif

#ifndef PSA_KEY_TYPE_IS_DH_KEY_PAIR
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_IS_DH_KEY_PAIR(type)");
#endif

#ifndef PSA_KEY_TYPE_IS_DH_PUBLIC_KEY
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_IS_DH_PUBLIC_KEY(type)");
#endif

#ifndef PSA_KEY_TYPE_IS_ECC
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_IS_ECC(type)");
#endif

#ifndef PSA_KEY_TYPE_IS_ECC_KEY_PAIR
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_IS_ECC_KEY_PAIR(type)");
#endif

#ifndef PSA_KEY_TYPE_IS_ECC_PUBLIC_KEY
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_IS_ECC_PUBLIC_KEY(type)");
#endif

#ifndef PSA_KEY_TYPE_IS_KEY_PAIR
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_IS_KEY_PAIR(type)");
#endif

#ifndef PSA_KEY_TYPE_IS_PUBLIC_KEY
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_IS_PUBLIC_KEY(type)");
#endif

#ifndef PSA_KEY_TYPE_IS_RSA
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_IS_RSA(type)");
#endif

#ifndef PSA_KEY_TYPE_IS_UNSTRUCTURED
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_IS_UNSTRUCTURED(type)");
#endif

#ifdef SPEC_PSA_KEY_TYPE_KEY_PAIR_OF_PUBLIC_KEY
    if (PSA_KEY_TYPE_KEY_PAIR_OF_PUBLIC_KEY(type) != SPEC_PSA_KEY_TYPE_KEY_PAIR_OF_PUBLIC_KEY(type)) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_TYPE_KEY_PAIR_OF_PUBLIC_KEY(type)");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_KEY_PAIR_OF_PUBLIC_KEY(type)");
#endif

#ifdef SPEC_PSA_KEY_TYPE_NONE
    if (PSA_KEY_TYPE_NONE != SPEC_PSA_KEY_TYPE_NONE) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_TYPE_NONE");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_NONE");
#endif

#ifdef SPEC_PSA_KEY_TYPE_PUBLIC_KEY_OF_KEY_PAIR
    if (PSA_KEY_TYPE_PUBLIC_KEY_OF_KEY_PAIR(type) != SPEC_PSA_KEY_TYPE_PUBLIC_KEY_OF_KEY_PAIR(type)) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_TYPE_PUBLIC_KEY_OF_KEY_PAIR(type)");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_PUBLIC_KEY_OF_KEY_PAIR(type)");
#endif

#ifdef SPEC_PSA_KEY_TYPE_RAW_DATA
    if (PSA_KEY_TYPE_RAW_DATA != SPEC_PSA_KEY_TYPE_RAW_DATA) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_TYPE_RAW_DATA");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_RAW_DATA");
#endif

#ifdef SPEC_PSA_KEY_TYPE_RSA_KEY_PAIR
    if (PSA_KEY_TYPE_RSA_KEY_PAIR != SPEC_PSA_KEY_TYPE_RSA_KEY_PAIR) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_TYPE_RSA_KEY_PAIR");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_RSA_KEY_PAIR");
#endif

#ifdef SPEC_PSA_KEY_TYPE_RSA_PUBLIC_KEY
    if (PSA_KEY_TYPE_RSA_PUBLIC_KEY != SPEC_PSA_KEY_TYPE_RSA_PUBLIC_KEY) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_TYPE_RSA_PUBLIC_KEY");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_RSA_PUBLIC_KEY");
#endif

#ifdef SPEC_PSA_KEY_TYPE_SM4
    if (PSA_KEY_TYPE_SM4 != SPEC_PSA_KEY_TYPE_SM4) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_TYPE_SM4");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_TYPE_SM4");
#endif

#ifdef SPEC_PSA_KEY_USAGE_CACHE
    if (PSA_KEY_USAGE_CACHE != SPEC_PSA_KEY_USAGE_CACHE) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_USAGE_CACHE");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_USAGE_CACHE");
#endif

#ifdef SPEC_PSA_KEY_USAGE_COPY
    if (PSA_KEY_USAGE_COPY != SPEC_PSA_KEY_USAGE_COPY) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_USAGE_COPY");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_USAGE_COPY");
#endif

#ifdef SPEC_PSA_KEY_USAGE_DECRYPT
    if (PSA_KEY_USAGE_DECRYPT != SPEC_PSA_KEY_USAGE_DECRYPT) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_USAGE_DECRYPT");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_USAGE_DECRYPT");
#endif

#ifdef SPEC_PSA_KEY_USAGE_DERIVE
    if (PSA_KEY_USAGE_DERIVE != SPEC_PSA_KEY_USAGE_DERIVE) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_USAGE_DERIVE");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_USAGE_DERIVE");
#endif

#ifdef SPEC_PSA_KEY_USAGE_ENCRYPT
    if (PSA_KEY_USAGE_ENCRYPT != SPEC_PSA_KEY_USAGE_ENCRYPT) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_USAGE_ENCRYPT");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_USAGE_ENCRYPT");
#endif

#ifdef SPEC_PSA_KEY_USAGE_EXPORT
    if (PSA_KEY_USAGE_EXPORT != SPEC_PSA_KEY_USAGE_EXPORT) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_USAGE_EXPORT");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_USAGE_EXPORT");
#endif

#ifdef SPEC_PSA_KEY_USAGE_SIGN_HASH
    if (PSA_KEY_USAGE_SIGN_HASH != SPEC_PSA_KEY_USAGE_SIGN_HASH) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_USAGE_SIGN_HASH");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_USAGE_SIGN_HASH");
#endif

#ifdef SPEC_PSA_KEY_USAGE_SIGN_MESSAGE
    if (PSA_KEY_USAGE_SIGN_MESSAGE != SPEC_PSA_KEY_USAGE_SIGN_MESSAGE) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_USAGE_SIGN_MESSAGE");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_USAGE_SIGN_MESSAGE");
#endif

#ifdef SPEC_PSA_KEY_USAGE_VERIFY_HASH
    if (PSA_KEY_USAGE_VERIFY_HASH != SPEC_PSA_KEY_USAGE_VERIFY_HASH) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_USAGE_VERIFY_HASH");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_USAGE_VERIFY_HASH");
#endif

#ifdef SPEC_PSA_KEY_USAGE_VERIFY_MESSAGE
    if (PSA_KEY_USAGE_VERIFY_MESSAGE != SPEC_PSA_KEY_USAGE_VERIFY_MESSAGE) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_KEY_USAGE_VERIFY_MESSAGE");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_KEY_USAGE_VERIFY_MESSAGE");
#endif

#ifndef PSA_MAC_LENGTH
    printf("WARN:  %s is not defined\n", "PSA_MAC_LENGTH(key_type, key_bits, alg)");
#endif

#ifndef PSA_MAC_MAX_SIZE
    printf("WARN:  %s is not defined\n", "PSA_MAC_MAX_SIZE");
#endif

#ifndef PSA_MAC_OPERATION_INIT
    printf("WARN:  %s is not defined\n", "PSA_MAC_OPERATION_INIT");
#endif

#ifndef PSA_RAW_KEY_AGREEMENT_OUTPUT_MAX_SIZE
    printf("WARN:  %s is not defined\n", "PSA_RAW_KEY_AGREEMENT_OUTPUT_MAX_SIZE");
#endif

#ifndef PSA_RAW_KEY_AGREEMENT_OUTPUT_SIZE
    printf("WARN:  %s is not defined\n", "PSA_RAW_KEY_AGREEMENT_OUTPUT_SIZE(key_type, key_bits)");
#endif

#ifndef PSA_SIGNATURE_MAX_SIZE
    printf("WARN:  %s is not defined\n", "PSA_SIGNATURE_MAX_SIZE");
#endif

#ifndef PSA_SIGN_OUTPUT_SIZE
    printf("WARN:  %s is not defined\n", "PSA_SIGN_OUTPUT_SIZE(key_type, key_bits, alg)");
#endif

#ifdef SPEC_PSA_SUCCESS
    if (PSA_SUCCESS != SPEC_PSA_SUCCESS) {
        fprintf(stderr, "ERROR: %s doesn't match value in spec\n", "PSA_SUCCESS");
        ret = 1;
    }
#else
    printf("WARN:  %s is not defined\n", "PSA_SUCCESS");
#endif

#ifndef PSA_TLS12_PSK_TO_MS_PSK_MAX_SIZE
    printf("WARN:  %s is not defined\n", "PSA_TLS12_PSK_TO_MS_PSK_MAX_SIZE");
#endif


    return ret;
}
